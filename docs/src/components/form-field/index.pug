extends /layouts/component-page

prepend head
    - ID = 'fab'
    - TITLE = 'FAB'
    - DESC = 'Floating action buttons represents the primary action in an application.'

block main
    +section('Basic')
        +mdc-fab('star')(ripple)

    +section('Mini')
        +mdc-fab('star')(mini ripple)

    +section('Icon with label')
        +mdc-fab('add', 'Create')(ripple)

    +section('Label with icon')
        +mdc-fab()(extended ripple)
            +mdc-fab__label('Create')
            +mdc-fab__icon('add')

    +section('Just label')
        +mdc-fab(null, 'Create')(ripple)